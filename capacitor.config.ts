import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'animalchecking.cl',
  appName: 'Animal Checking',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
